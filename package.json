{
  "name": "file-upload-server",
  "version": "1.0.0",
  "description": "simple server to handle files",
  "main": "app.js",
  "type": "module",
  "scripts": {
    "build": "docker build -t file-upload-server .",
    "run": "docker run --name fileServer -p 3000:3000 file-upload-server"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/toddscottik/file-upload-server.git"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.21.2",
    "multer": "^1.4.5-lts.1"
  }
}
